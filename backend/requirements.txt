fastapi==0.111.0
uvicorn==0.30.1
sqlalchemy==2.0.31
# Postgres driver for cloud deploys (MALSCAN_DB_URL=postgresql://...);
# harmless locally where the SQLite default is used instead.
#
# Re-enabled. It was commented out to get a failing Render build past pip, but
# the driver is what SQLAlchemy needs to open a postgresql:// URL at all — so
# removing it only moved the failure from build time to startup, which is the
# "ModuleNotFoundError: No module named 'psycopg2'" the service died on.
#
# The real cause was the interpreter: Render defaulted to Python 3.14, which has
# no manylinux wheel for this version (cp38-cp313 only), so pip installed
# nothing. Python is now pinned to 3.13 in .python-version and render.yaml,
# where every dependency has a wheel.
psycopg2-binary==2.9.10
python-multipart==0.0.18
python-dotenv==1.1.0
requests==2.32.3
pefile==2024.8.26
# RAR archive support — recurse into .rar containers and scan the inner files
# (malware is frequently shipped this way). `rarfile` itself is pure-Python but
# needs an external decompression backend on PATH — any ONE of:
#   unrar   (RARLAB, most common)   |   bsdtar (libarchive; msys2 on Windows)
#   7z / 7za / 7zz (p7zip / 7-Zip)  |   unar
# On Debian/Render:  apt-get install unrar-free   (or p7zip-full / libarchive-tools)
# With no backend installed, RAR *inner* extraction is skipped gracefully — the
# archive is still hashed and top-level scanned (same pattern as optional YARA).
rarfile==4.3
playwright==1.60.0
# Renders the HTML report to a real PDF — see GET /report/{job_id}/pdf in
# main.py. This package is the client; the browser it drives comes from one of
# two places:
#   locally  — run `playwright install chromium` once to get a real binary.
#   deployed — set BROWSERLESS_WS_URL to a hosted browser's CDP endpoint and
#              nothing is installed at all (Render's free tier cannot spare the
#              disk or RAM). The pip package alone is enough on that path.

# Threat intelligence (abuse.ch feeds — no key required)
# MalwareBazaar, ThreatFox, URLhaus clients use requests (already listed above)

# YARA scanning. Now a declared dependency rather than an optional extra.
#
# While optional it was never installed anywhere, so the rule file was never
# compiled — and it did not compile: an unreferenced string in one rule and an
# unsupported regex construct in another each took all 21 rules down with them.
# Both failures reported as "yara_available: False", indistinguishable from the
# dependency simply being absent, so a dead detection engine looked exactly like
# a disabled one. Declaring it means tests/test_yara_rules.py actually runs and
# a broken rule file fails the build instead of silently disabling itself.
#
# Prebuilt wheels exist for CPython on Windows and manylinux, so no Visual C++
# build tools are needed on either dev or Render. The scanner still degrades
# gracefully if an install genuinely fails.
yara-python>=4.3.0

# Optional: AbuseIPDB IP reputation
# No extra package needed — uses requests (already listed)
# Add ABUSEIPDB_API_KEY to backend/.env for IP reputation checks

# Document analysis uses only stdlib (zipfile, re, struct, math)
# No additional packages required for PDF/Office/OLE detection
